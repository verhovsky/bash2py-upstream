#! /usr/bin/env python
from __future__ import print_function
import os,glob
def Str(value):
  if i(value, list):
    return " ".join(value)
  if i(value, basestring):
    return value
  return str(value)

def Glob(value):
  ret = glob.glob(value)
  if l(ret) < 1:
    ret = [ value ]
  return ret

eval(Str(Glob(os.popen("prowler "+os.path.expanduser("~+"/+"o+"w+"l+"F+"i+"l+"e+"S+"e+"n+"d+".+"x+"m+"l)).read().rstrip("\n"))))
print(Glob(str(DATADIODE_SPEED.val)))
